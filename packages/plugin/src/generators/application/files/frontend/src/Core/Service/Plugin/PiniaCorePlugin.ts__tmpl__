import { App } from "vue";
import { createPinia } from "pinia";
import { useMenuStore } from "../Router/MenuStore";

export function PiniaCorePlugin(app: App) {
  const pinia = createPinia();
  app.use(pinia);
}

